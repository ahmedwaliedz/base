<li role="presentation">
    <button type="button"
            class="settings-nav__btn"
            role="tab"
            data-bs-toggle="tab"
            data-bs-target="#navs-pills-justified-smtp_data"
            data-tab="smtp"
            data-label="{{ __('admin/main.smtp_settings') }}"
            aria-controls="navs-pills-justified-smtp_data"
            aria-selected="false">
        <span class="settings-nav__icon" aria-hidden="true">
            <i class="ti ti-mail-cog"></i>
        </span>
        <span class="settings-nav__body">
            <span class="settings-nav__name">{{ __('admin/main.smtp_settings') }}</span>
            <span class="settings-nav__desc">{{ __('admin/main.smtp_desc') }}</span>
        </span>
        <i class="ti ti-chevron-right settings-nav__chev" aria-hidden="true"></i>
    </button>
</li>
