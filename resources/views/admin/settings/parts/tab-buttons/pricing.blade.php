<li role="presentation">
    <button type="button"
            class="settings-nav__btn"
            role="tab"
            data-bs-toggle="tab"
            data-bs-target="#navs-pills-justified-pricing"
            data-tab="pricing"
            data-label="{{ __('admin/main.pricing') }}"
            aria-controls="navs-pills-justified-pricing"
            aria-selected="false">
        <span class="settings-nav__icon" aria-hidden="true">
            <i class="ti ti-receipt-2"></i>
        </span>
        <span class="settings-nav__body">
            <span class="settings-nav__name">{{ __('admin/main.pricing') }}</span>
            <span class="settings-nav__desc">{{ __('admin/main.pricing_desc') }}</span>
        </span>
        <i class="ti ti-chevron-right settings-nav__chev" aria-hidden="true"></i>
    </button>
</li>
