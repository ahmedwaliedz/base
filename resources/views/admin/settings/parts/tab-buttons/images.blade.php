<li role="presentation">
    <button type="button"
            class="settings-nav__btn"
            role="tab"
            data-bs-toggle="tab"
            data-bs-target="#navs-pills-justified-images"
            data-tab="images"
            data-label="{{ __('admin/main.images') }}"
            aria-controls="navs-pills-justified-images"
            aria-selected="false">
        <span class="settings-nav__icon" aria-hidden="true">
            <i class="ti ti-photo"></i>
        </span>
        <span class="settings-nav__body">
            <span class="settings-nav__name">{{ __('admin/main.images') }}</span>
            <span class="settings-nav__desc">{{ __('admin/main.images_desc') }}</span>
        </span>
        <i class="ti ti-chevron-right settings-nav__chev" aria-hidden="true"></i>
    </button>
</li>
