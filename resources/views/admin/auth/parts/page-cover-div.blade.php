{{--
    Cover panel is no longer used.
    Login page now shows a single centered form on a soft animated background.
    File kept for backwards compatibility — safe to delete if not referenced elsewhere.
--}}